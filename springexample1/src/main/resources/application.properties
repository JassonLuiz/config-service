spring.application.name=spring-example-1
server.port=8085

config.api.username=admin
config.api.password=admin123
config.api.base-url=http://localhost:8080

# Configuração da aplicação
config.namespace=my-app
config.environment=dev

# Configuração do Kafka para a biblioteca
config.client.kafka.enabled=true
config.client.kafka.topics=config.my-app.dev
config.client.kafka.group-id=${spring.application.name}
config.client.kafka.bootstrap-servers=localhost:9092

config.client.sync.timeout=30s
config.client.sync.batch-size=50
config.client.sync.retry.max-attempts=3
config.client.sync.retry.backoff=1s

# Redis
spring.redis.host=localhost
spring.redis.port=6379
spring.redis.database=0
spring.redis.timeout=2000ms

# Kafka - Configuração base
spring.kafka.bootstrap-servers=localhost:9092

# Kafka Consumer
spring.kafka.consumer.bootstrap-servers=localhost:9092
spring.kafka.consumer.group-id=${spring.application.name}
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.enable-auto-commit=false

# Deserializadores com tratamento de erro
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
spring.kafka.consumer.properties.spring.deserializer.value.delegate.class=org.springframework.kafka.support.serializer.JsonDeserializer

# Configuração JsonDeserializer para aceitar múltiplos tipos
spring.kafka.consumer.properties.spring.json.trusted.packages=io.github.clientlibrary.client_library.event
spring.kafka.consumer.properties.spring.json.use.type.info.headers=false
spring.kafka.consumer.properties.spring.json.value.default.type=java.lang.Object

# Timeout e configurações de performance
spring.kafka.consumer.properties.session.timeout.ms=30000
spring.kafka.consumer.properties.heartbeat.interval.ms=10000
spring.kafka.consumer.properties.max.poll.interval.ms=300000
spring.kafka.consumer.properties.max.poll.records=10

# Logs para debugging (opcional)
logging.level.io.github.clientlibrary.client_library.consumer=INFO
logging.level.io.github.springexample.springexample1=DEBUG
logging.level.org.springframework.kafka=WARN
logging.level.org.apache.kafka=WARN